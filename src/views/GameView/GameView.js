import React from 'react';
import {
  Grid,
  Title,
} from 'beautiful-react-ui'

const GameView = ({ user, history }) => {
  return (
    <Grid>
      <Grid.Column sm="12">
        <Title>GameView</Title>
      </Grid.Column>
    </Grid>
  );
}

export default GameView;